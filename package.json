{
  "name": "railway-tickets-api",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT db.json --cors"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}